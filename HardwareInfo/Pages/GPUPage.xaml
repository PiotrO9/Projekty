<Page x:Class="HardwareInfo.Pages.GPUPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HardwareInfo.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GPUPage">

    <Grid x:Name="MianGrid" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Frame x:Name="MainFrame" Grid.Column="0" Grid.Row="0"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Button x:Name="UpButton" Content="UP" VerticalAlignment="Bottom" HorizontalAlignment="Center" Padding="15,10,15,10" Margin="0,0,0,15" Grid.Row="0" Click="UpButton_Click"/>
            <Button x:Name="DownButton" Content="DOWN" VerticalAlignment="Top" HorizontalAlignment="Center" Padding="5,10,5,10" Margin="0,15,0,15" Grid.Row="1" Click="DownButton_Click"/>
        </Grid>

    </Grid>
</Page>
